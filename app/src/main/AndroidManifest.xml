<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.three38inc.apps.shellsapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.DesignDemo"
        tools:replace="android:theme">
        <activity
            android:name=".SplashScreenActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            </activity>
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.DesignDemo">
        </activity>
        <activity
            android:name=".EventDetailsActivity"
            android:label="Event Details"
            android:theme="@style/Theme.DesignDemo" />
        <activity
            android:name=".ScheduleActivity"
            android:label="Schedule"
            android:theme="@style/Theme.DesignDemo" />

        <receiver android:name=".customHandler" />

        <activity
            android:name=".ResultsActivity"
            android:label="Results"
            android:theme="@style/Theme.DesignDemo" />
        <activity
            android:name=".ChatActivity"
            android:label="Chat with Hash!"
            android:theme="@style/Theme.DesignDemo"></activity>
        <activity
            android:name="com.three38inc.apps.shellsscan.UnityPlayerActivity"
            android:label="Shells Promotion" />
    </application>

</manifest>